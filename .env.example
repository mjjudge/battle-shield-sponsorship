# Stripe API keys
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# WordPress database (for local development)
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=
DB_HOST=localhost

# Plugin settings
PLUGIN_ENV=development
